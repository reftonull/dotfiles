# tree shortcuts — these expand so you can see the full command before running
abbr -a tn tree new
abbr -a tl tree ls
abbr -a tr tree rm
abbr -a tg tree go
abbr -a ts tree sync
abbr -a tp tree submit
abbr -a tps tree submit --stack
